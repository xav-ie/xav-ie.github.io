---
import { buildHeartSvg, type HeartSvgOptions } from '../heart-svg';

const { size = '6em', title = 'Heart' } = Astro.props as HeartSvgOptions;

const svg = buildHeartSvg({ size, title });
---

<Fragment set:html={svg} />
